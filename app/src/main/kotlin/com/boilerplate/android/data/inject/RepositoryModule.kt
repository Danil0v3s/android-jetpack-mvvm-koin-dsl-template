package com.boilerplate.android.data.inject

import org.koin.dsl.module.module

val repositoryModule = module {
//    single { CategoryRepository(api = get(), categoryDao = get()) }
//    single { CartRepository(cartDao = get()) }
}